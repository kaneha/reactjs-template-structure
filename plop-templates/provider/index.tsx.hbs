import { createContext, useContext, useState } from 'react';
import { {{pascalCase name}}ProviderProps } from './props';

const {{pascalCase name}}Context = createContext<{{pascalCase name}}ProviderProps>({
  state: '',
});

export const use{{pascalCase name}} = () => useContext({{pascalCase name}}Context);

export const {{pascalCase name}}Provider: React.FC = ({ children }) => {
  const [state, setState] = useState('');
  return (
    <{{pascalCase name}}Context.Provider value=\{{ state }}>{children}</{{pascalCase name}}Context.Provider>
  );
};