import { {{pascalCase name}}Props } from './{{pascalCase name}}.props';
import { Container } from './{{pascalCase name}}.style';

const {{pascalCase name}}: React.FC<{{pascalCase name}}Props> = (props) => {
  const {
    children,
    ...{{name}}Props
  } = props;

  return <Container {...{{name}}Props}>{children}</Container>
};

{{pascalCase name}}.defaultProps = {

}

{{pascalCase name}}.displayName = '{{pascalCase name}}';

export default {{pascalCase name}};