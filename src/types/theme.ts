export interface ITheme {
  color: {
    primary: string;
    default: string;
  };
  zIndex: {
    speedDial: number;
    appBar: number;
    toast: number;
    tooltip: number;
    modal: number;
  };
}
